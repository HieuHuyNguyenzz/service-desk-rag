services:
  service-desk-rag:
    build: .
    container_name: service-desk-rag-sync
    env_file:
      - .env
    dns:
      - 10.255.244.100
      - 8.8.8.8
    extra_hosts:
      - "servicedesk.viettelcyber.com:10.255.244.100"
    volumes:
      - ./sync_state.json:/app/sync_state.json
    restart: unless-stopped
